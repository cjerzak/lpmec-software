% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpme_Multivariate.R
\name{lpmec_multivariate}
\alias{lpmec_multivariate}
\title{Aggregated multivariate latent-predictor correction}
\usage{
lpmec_multivariate(
  Y,
  observables,
  covariates = NULL,
  observables_groupings = NULL,
  make_observables_groupings = FALSE,
  n_boot = 32L,
  n_partition = 10L,
  partition_aggregation = "median",
  partition_aggregation_probs = c(0.01, 0.99),
  boot_basis = seq_along(Y),
  return_intermediaries = TRUE,
  estimation_method = "em",
  latent_estimation_fn = NULL,
  ordinal = FALSE,
  mcmc_control = list(backend = "pscl", n_samples_warmup = 500L, n_samples_mcmc = 1000L,
    batch_size = 512L, chain_method = "parallel", subsample_method = "full",
    anchor_parameter_id = NULL, n_thin_by = 1L, n_chains = 2L, outcome_prior =
    list(calibration = "data"), joint2_prior = list()),
  min_split_correlation = 0,
  conda_env = "lpmec",
  conda_env_required = FALSE
)
}
\arguments{
\item{Y}{Numeric outcome vector.}

\item{observables}{A list of matrices or data frames, one per latent predictor.}

\item{covariates}{Optional matrix or data frame of observed covariates.}

\item{observables_groupings}{Optional list of grouping vectors, one per latent
predictor. Defaults to the column names of each observable matrix.}

\item{make_observables_groupings}{Logical scalar or vector passed to
\code{\link{lpmec_onerun}} for each latent predictor.}

\item{n_boot}{Non-negative integer number of row-bootstrap iterations.}

\item{n_partition}{Positive integer number of split-half partitions per
original or bootstrap sample.}

\item{partition_aggregation}{Aggregation strategy across partitions. See
\code{\link{lpmec}}.}

\item{partition_aggregation_probs}{Quantile probabilities for winsorized or
trimmed partition aggregation.}

\item{boot_basis}{Optional vector of indices or strata for row bootstrap.}

\item{return_intermediaries}{Logical. If \code{TRUE}, returns per-run
coefficient matrices.}

\item{estimation_method}{Character scalar or vector passed to
\code{\link{lpmec_onerun}} for each latent predictor.}

\item{latent_estimation_fn}{Optional function or list of functions used when
\code{estimation_method = "custom"}.}

\item{ordinal}{Logical scalar or vector passed to \code{\link{lpmec_onerun}}.}

\item{mcmc_control}{List passed to \code{\link{lpmec_onerun}}.}

\item{min_split_correlation}{Minimum allowed split-half correlation. The
correction is defined only for positive componentwise correlations.}

\item{conda_env}{Character string naming the conda environment for MCMC methods.}

\item{conda_env_required}{Logical indicating whether \code{conda_env} is required.}
}
\value{
A list of class \code{lpmec_multivariate} containing aggregated
  uncorrected OLS and corrected IV latent coefficients with bootstrap
  uncertainty summaries when \code{n_boot >= 1}.
}
\description{
Runs \code{\link{lpmec_multivariate_onerun}} over repeated split-half
partitions and optional row bootstrap samples.
}
